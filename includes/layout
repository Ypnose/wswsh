# Page content

function page_head {
	print '<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8" />'
	if (( WSH_CSS == 1 )); then
		print "\t<link rel=\"stylesheet\" type=\"text/css\" href=\"/${WSH_CSSF}\" />"
	fi
	if [[ -n $WSH_TIT ]]; then
		print "\t<title>${WSH_TIT}${FILETIT:+ | $FILETIT}</title>"
	else
		print "\t<title>${FILETIT}</title>"
	fi
	print '</head>
<body>'
}

function page_foot {
	print '</body>
</html>'
}
